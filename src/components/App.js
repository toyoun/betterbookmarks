import React, { Component } from 'react';
import './App.css';
import Viewport from './Viewport';

class App extends Component {
  render() {
    return (
      <Viewport />
    );
  }
}

export default App;
